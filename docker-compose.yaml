name: python-base

services:
  dev:
    build:
      context: .
      target: dev
    volumes:
      - .:/app
      - uv-cache:/root/.cache/uv
    environment:
      PYTHONUNBUFFERED: "1"
    command: ["sleep", "infinity"]
    tty: true
    stdin_open: true

volumes:
  uv-cache:
